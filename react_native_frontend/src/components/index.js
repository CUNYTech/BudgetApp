import Form, { Input, Button, BackButton, Menu } from './form';
import Partials, { AlertStatus, Logo } from './partials';
import Text, { TextFont, Heading } from './text';
import { BudgetSnapshot, GoalsSnapshot, FriendsSnapshot, PointsSnapshot } from './snapshots';
import { AppRouter, AppSideMenu } from './nav';


export {
    Form, Input, Button, BudgetSnapshot,
    Partials, AlertStatus, BackButton, Logo,
    Text, TextFont, Heading, GoalsSnapshot,
    FriendsSnapshot, PointsSnapshot, Menu,
    AppSideMenu, AppRouter,
};

export default {
  Form,
  Input,
  Button,
  Partials,
  AlertStatus,
  BackButton,
  Logo,
  Text,
  TextFont,
  Heading,
  BudgetSnapshot,
  GoalsSnapshot,
  FriendsSnapshot,
  PointsSnapshot,
  Menu,
  AppSideMenu,
  AppRouter,
};
